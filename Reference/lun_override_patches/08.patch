--- mod-2cae441/common/military_industrial_organization/organizations/00_generic_organization.txt
+++ mod-aac2956/common/military_industrial_organization/organizations/00_generic_organization.txt
@@ -15,6 +15,7 @@
 	icon = GFX_idea_generic_tank_manufacturer_1
 
 	allowed = {
+		NOT = { original_tag = LUN } # Renko: LUN uses its exclusive MIO
 		NOT = { OR = { original_tag = GER original_tag = KYO original_tag = ENG original_tag = SOV original_tag = FRA original_tag = ITA original_tag = JAP original_tag = USA original_tag = BEL original_tag = CZE original_tag = ROM  original_tag = HUN } }
 		NOT = {
 			AND = {
@@ -2454,6 +2455,7 @@
 	icon = GFX_idea_generic_naval_manufacturer_1
 
 	allowed = {
+		NOT = { original_tag = LUN } # Renko: LUN uses its exclusive MIO
 		NOT = { OR = { original_tag = GER original_tag = KYO tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = BEL tag = CAN tag = SWE tag = HUN tag = CZE tag = ROM tag = YUG tag = POR tag = GRE tag = TUR tag = LAT tag = EST } }
 		NOT = { OR = { tag = SPR tag = SPA tag = SPB tag = SPC tag = SPD } }
 		NOT = {
@@ -3964,6 +3966,7 @@
 	icon = GFX_idea_generic_air_manufacturer_1
 
 	allowed = {
+		NOT = { original_tag = LUN } # Renko: LUN uses its exclusive MIO
 		NOT = { 
 			OR = { original_tag = GER original_tag = KYO tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = BEL tag = CAN tag = YUG tag = ROM tag = CZE tag = HUN tag = CHI tag = MAN tag = HOL tag = BUL tag = GRE tag = TUR tag = LAT tag = LIT 
 			} 
@@ -6796,6 +6799,7 @@
 	icon = GFX_idea_generic_artillery_manufacturer_2
 
 	allowed = {
+		NOT = { original_tag = LUN } # Renko: LUN uses its exclusive MIO
 		NOT = { OR = { original_tag = GER original_tag = KYO tag = ENG tag = SOV tag = FRA tag = ITA tag = USA tag = JAP tag = CZE tag = ROM tag = MEX } }
 		NOT = {
 			AND = {
@@ -7200,6 +7204,7 @@
 	icon = GFX_idea_generic_infantry_equipment_manufacturer_2
 
 	allowed = {
+		NOT = { original_tag = LUN } # Renko: LUN uses its exclusive MIO
 		NOT = { OR = { original_tag = GER original_tag = KYO tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = BEL tag = CZE tag = HUN tag = YUG tag = ROM tag = MEX tag = TUR tag = EST } }
 		NOT = {
 			AND = {
@@ -7846,6 +7851,7 @@
 	icon = GFX_idea_generic_motorized_equipment_manufacturer_3
 
 	allowed = {
+		NOT = { original_tag = LUN } # Renko: LUN uses its exclusive MIO
 		NOT = { OR = { original_tag = GER original_tag = KYO tag = ENG tag = SOV tag = FRA tag = ITA tag = JAP tag = USA tag = CZE tag = ROM tag = MEX } }
 		NOT = {
 			AND = {
