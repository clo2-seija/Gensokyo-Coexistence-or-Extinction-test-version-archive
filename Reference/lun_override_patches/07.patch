--- vanilla-20260910/common/ai_navy/taskforce/generic_taskforce_templates.txt
+++ mod-aac2956/common/ai_navy/taskforce/generic_taskforce_templates.txt
@@ -1,6 +1,7 @@
 StrikeForce_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
@@ -46,6 +47,7 @@
 PatrolReconForce_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
@@ -80,6 +82,7 @@
 PatrolDominanceForce_CA_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
@@ -112,6 +115,7 @@
 PatrolDominanceForce_BC_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
@@ -144,6 +148,7 @@
 ConvoyRaiding_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
@@ -174,6 +179,7 @@
 ConvoySurfaceRaiding_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
@@ -204,6 +210,7 @@
 ConvoyEscort_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
@@ -233,6 +240,7 @@
 MineLaying_1 = {
 	allowed = {
 		NOT = {
+			original_tag = LUN
 			original_tag = JAP
 			original_tag = ENG
 			original_tag = SOV
