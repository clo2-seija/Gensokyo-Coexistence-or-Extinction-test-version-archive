# 守矢规划器新方案与对称布局

日期：2026-09-16。作者标识：Renko。

按用户授权，将完整 13 节点守矢计划写入规划器 JSON，保留共同入口与付丧神分支，移除过时的守矢草稿节点；历史 baseline 不变。

## 改动

- 同步 baseline.json 的可编辑 nodes 与 HTML 内嵌数据，并生成独立 draft.json 导入文件。
- 以 x=5 为中轴对称布局三组特色国策，守矢前置纵向跨度 1～2；仅三个追随方向彼此互斥。
- 共享特色开放给早苗，独占舰艇仅允许对应神明路线；三条路线均可完成收束。
- 未定工期存为 null，编辑器校验、输入、显示和导出均支持待定；新建节点也不再默认指定工期。
- 6 项待图节点留空，7 项复用专用 GFX；共同入口内容概述依据已实施起点计划更新，历史源码仍作为旧快照保留。
- 更新 README 和通用计划的交接状态；新缓存命名空间不删除旧草稿。

## 验证

JSON／HTML 数据一致，JavaScript 语法通过；调用实际 importText 的内存测试完成导入往返，拒绝非法工期及循环前置；历史 baseline 与付丧神其他内容保留检查通过。对称坐标、1～2 的守矢前置跨度、唯一互斥组和三条路线可达性通过。

浏览器安全策略拦截 file 地址，未完成浏览器点击导入或页面视觉验证。已将更新 JSON 内嵌为 HTML 默认草稿，可直接打开更新版或手动导入 draft.json。

不涉及游戏脚本、本地化或图片变更；未运行游戏。本轮按用户后续授权提交推送。

## 用户修订稿接收

用户提供 Renko_moriya_naval_focus_draft (1).json，已原样保存为 development_plans/Renko_moriya_naval_focus_draft.json；与下载原件哈希一致、21 节点、JSON 可解析。该文件为最新人工修订稿。HTML 和 baseline.json 仍保留上一轮助手布局，尚未同步用户本次调整；上文结构验证针对上一轮助手草稿，不代表对用户修订布局作了相同验证。
