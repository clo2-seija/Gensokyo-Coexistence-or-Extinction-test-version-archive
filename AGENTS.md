# 幻想乡 MOD 协作开发规则

## 工作范围

- 读取、编辑或创建文件前，先检查并遵守当前目录及相关父目录的 AGENTS.md。
- 修改前恢复目标文件及直接依赖的上下文，不凭不完整片段猜测实现。
- 仅设计、讨论、审阅或调查的任务保持只读，不顺带修改游戏内容。
- 用户纠正某个方向时，只撤销对应错误修改，保留仍然有效的工作。
- 尊重其他作者的代码与未提交修改，不覆盖、清理或重写无关内容。
- 面向作者的文档、修改记录与说明默认使用简体中文。

## 分支与提交

- 永远不在 main 分支上提交，也不直接向远端 main 推送或合并。
- 每项独立修改任务开始前，先检查当前分支、工作区和已有改动，再新建专用分支；同一任务的后续调整继续使用该任务分支。
- 分支默认命名为 codex/renko-<任务简述>。
- 基于哪个分支或提交开始，应结合上游状态和任务依赖确定并记录；不盲目从已经过时的本地 main 开始。
- 新建分支前妥善识别已有未提交文件，不擅自 stash、删除或混入新任务。
- 工坊补全资源与后续功能修改分别记录，避免混成一次无法审阅的改动。
- 只暂存本任务涉及的文件，避免使用无差别暂存。
- 提交、推送、创建 PR 或发布按用户授权执行；请求修改本身不等于授权发布。
- main 的最终合入由项目协作者按协作流程处理。

## 修改日志

- 使用 development_logs/ 保存开发修改记录。
- 每项实际落地的修改必须有对应记录，包括脚本、资源、本地化、配置、技能和参考文档的修改。
- 一项任务使用一份 YYYY-MM-DD_简述.md；同一任务的连续调整更新同一份记录，不要求每次保存文件都创建新日志。
- 日志至少包含：
  - 日期、任务名称、作者标识 Renko。
  - 工作分支与起始提交。
  - 修改目的及内容概括。
  - 修改文件清单：新增、修改、删除或重命名，使用仓库相对路径。
  - 验证方式、结果及尚未验证的部分。
  - 原版覆盖或外部参考来源，如有。
- 日志随对应改动一起提交，内容应描述最终实现。
- 日志自身注明记录文件路径即可，不递归创建“日志修改日志”。
- 未进行实机验证时明确写“未实机验证”；静态检查通过不等于游戏表现通过。

## 原版覆盖与追踪账本

- 原版安装目录作为只读依据，不直接修改原版游戏文件。
- 优先采用满足目标的最小覆盖范围，避免无必要地复制整份原版文件。
- 如果需要新增仓库中不存在的原版覆盖文件，先说明源文件、覆盖原因和影响范围；用户已明确授权该覆盖时直接执行，否则先提交具体方案供确认。
- 使用 Reference/vanilla_override_ledger.md 维护覆盖账本。
- 每项覆盖至少记录：
  - 原版源文件相对路径与 MOD 目标路径。
  - 引入时的游戏版本、原版 Git 基线提交及文件哈希。
  - 覆盖原因、预期行为和相对原版的修改点。
  - 覆盖方式，以及涉及的 descriptor/replace_path。
  - 最近一次上游核对结果、验证状态和关联修改日志。
- 修改已有覆盖文件时同步更新账本；移除覆盖时保留移除记录。
- 原版文件需要纳入本机原版 Git 基线时，沿用 Japan_rework 的追踪方式，仅对明确涉及的源文件执行 git add -f，不夹带其他文件，也不擅自提交该基线。
- 游戏更新后按账本核对原版变化，以当前原版为依据重新应用已记录的修改点，避免直接覆盖掉 MOD 改动。
- 只有确实需要替换相应加载范围时才使用 replace_path；新增条目前写明用途，不把它作为复制原版文件的固定步骤。

## Japan_rework 经验与技能

- Japan_rework 测试版是本项目的主要外部经验来源，默认只读。
- 在当前设备定位 Japan_rework 根目录；当前布局下它与本项目同级。路径失效时先查实际位置，不新建空目录冒充参考来源。
- 按任务读取以下技能及必要的配套参考（路径相对 Japan_rework）：
  - 国策编写、国策图标：.agents/skills/hoi4-focus-authoring/SKILL.md
  - 决议编写、决议骨架与图标：.agents/skills/hoi4-decision-authoring/SKILL.md
  - AI 策略设计与案例：.agents/skills/hoi4-ai-strategy-authoring/SKILL.md；该技能尚未完成，仅使用已整理的相关内容。
- 通用经验入口为 Japan_rework/Reference/agent_reference_index.md；按需查阅变量、触发器生命周期、修正、本地化、建筑、defines、AI 策略及相关实现文件，不一次加载全部资料。
- 技能内 references/ 相对该技能所在目录解析。指向测试版案例及经验文档的路径相对 Japan_rework 解析；本次编辑目标始终相对幻想乡项目解析，遇到歧义先明确归属。
- 参考测试版时追踪所需 trigger、effect、flag、变量和事件依赖，不只复制表面代码。
- 借鉴通用语法、实现思路和经验证的经验；不自动继承日本专属路线、数值、文件名、发布流程或编码例外。
- 区分原版依据、测试版代码事实、设计推测与实机验证结论。对版本敏感或存在疑问的行为，核对当前原版。
- 幻想乡项目的明确约定优先于参考案例中的项目专属习惯。
- 未经明确要求，不修改或同步 Japan_rework 的技能、参考和游戏文件。

## Renko 命名规则

- 我们新增且可自主命名的脚本标识符统一使用 Renko_ 前缀，保留 Renko 的大小写。
- 适用于新增国策、决议、分类、国家精神、特质、scripted_trigger、scripted_effect、flag、变量及本地化键等。
- 新增事件 namespace 使用 Renko_主题，事件 ID 使用该 namespace。
- 引擎要求固定结构的名称保留必要结构，例如：GFX_Renko_example、Renko_events.1。
- 新增自有脚本文件优先使用 Renko_主题.txt；原版同路径覆盖文件及引擎固定文件名保持所需名称。
- 修改其他作者或原版已有定义时，保留原 ID，不为标记作者而重命名并破坏引用；通过修改日志记录作者和范围。
- 复制案例形成独立实现时，替换自有标识符并核对全部引用，不遗留测试版的 Rance_ 或其他作者的临时命名。
- 国家标签、固定语法键及对外部既有定义的引用遵循引擎与项目要求，不强行添加前缀。

## 编写与验证

- 本项目默认以新开局为编写和验证基线；未经明确要求，不新增旧存档迁移或兼容分支。
- 未经明确审批，不新增、扩展或占用 on_daily、on_monthly 钩子；优先使用决议、mission、事件和持久 flag 等范围更窄的路径。
- HOI4 语法以当前原版、项目实现和可靠参考为依据，不凭记忆发明键名、作用域或参数。
- 修改前确认文件编码和换行，保留已有文件格式，不因终端乱码而整份重写文件。
- 新建普通脚本和说明文档默认 UTF-8 无 BOM、LF；新建本地化默认 UTF-8 BOM、LF；引擎或项目已有明确要求时按要求处理。
- 新增内容沿用本项目的国家、角色、地理和叙事语境，玩家可见名称优先使用项目现有中文本地化。
- 英文本地化中，项目内使用的“莲子”统一译为 `Renko`；例如“莲子的工具箱”为 `Renko's Toolbox`。
- 不把玩家应知的实质奖励隐藏在 hidden_effect 中。
- 只运行与改动相关的检查；发现问题后再有针对性地扩展核对范围。
- 完成说明应包含修改结果、验证证据和实际限制。
- 完成任务回复时，必须附上本次关键代码的可点击文件链接，并定位到相关行，方便作者审查；仅修改文档时链接对应关键条目。
- 对登记的问题，修复后先标记“待检验”，经对应实机或日志证据确认后再标记解决。

## 国家存在性判定

- `exists` 只接收 `yes/no`；检查指定国家时使用 `LUN = { exists = yes }` 等国家作用域写法，禁止 `exists = LUN`。修改国家存在性条件后全查 `exists = TAG`，不要误改合法的 `country_exists = TAG`。
